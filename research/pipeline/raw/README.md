# Raw layer

The two Microsoft report PDFs (S01, S01b) are excluded from git to avoid repo bloat.
They are byte-recoverable from github.com/microsoft/ai-diffusion-report at the git HEAD
pinned in manifest/manifest.json, and their SHA-256 hashes are recorded there.
All other raw artifacts are committed immutably.
